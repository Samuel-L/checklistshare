import React from 'react';
import PropTypes from 'prop-types';

const styles = {
  base: {},
};

const Home = props => (
  <div></div>
);

Home.propTypes = {};

Home.defaultProps = {};

export default Home;
